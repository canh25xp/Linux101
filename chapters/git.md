# Version control with `git`

## Prerequisite

Since `git` is such a common if not the most common **version control** system,
I will presumed that you know at least these `git` basic commands:

- `git init`
- `git clone`
- `git add`
- `git commit`
- `git pull`
- `git push`
